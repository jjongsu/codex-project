# Shared Game Layer

- `components/`: shared HUD, result modal, score submit modal, Phaser wrapper
- `store/`: Zustand stores shared across game pages
- keep game-specific scene logic outside this folder
